{
  "name": "@email-filter/email-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo 'Skipping email-worker build (use deploy for Cloudflare)'",
    "build:cf": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231121.0",
    "fast-check": "^3.23.2",
    "typescript": "^5.3.2",
    "vitest": "^1.0.0",
    "wrangler": "^3.19.0"
  }
}
